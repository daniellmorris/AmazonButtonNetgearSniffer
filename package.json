{
  "name": "netgear_dash_detection",
  "version": "1.0.0",
  "description": "Detect dash button through the netger WRN2000v5 interface by periodically pulling from it. Every second. Possibly run from Pi.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.17.1",
    "nedb": "^1.8.0",
    "prompt-promise": "^1.0.3",
    "puppeteer": "^0.13.0"
  }
}
